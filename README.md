# Local Hosts Registry

This repository contains the host registry data for fischer.nyc network.

The `index.txt` file is automatically updated by the host-registry service and contains a formatted list of all hosts across all subnets.

Access via: `curl hosts.fischer.nyc`
